/**
 * Model-layer view of the domain schemas. The definitions live in lib/domain/schemas.ts so that
 * lib/rules can share the types without importing anything from lib/model (constitution III).
 */
export * from "@/lib/domain/schemas";
